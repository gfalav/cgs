<%= form_for(@tconstr) do |f| %>
  <% if @tconstr.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tconstr.errors.count, "error") %> prohibited this tconstr from being saved:</h2>

      <ul>
      <% @tconstr.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tipo %><br />
    <%= f.text_field :tipo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
